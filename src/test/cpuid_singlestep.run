source `dirname $0`/util.sh
# Try recording without CPUID fauilting and then replaying with it
GLOBAL_OPTIONS="--disable-cpuid-faulting --resource-path=${OBJDIR}"
record $TESTNAME
GLOBAL_OPTIONS=
debug cpuid_singlestep
